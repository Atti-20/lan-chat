# 项目协作约定

## 报告归档

- 所有分析、审查、验证及其他报告统一存放于 `docs/reports/<分类>/`，按主题分类。
- 文件名采用 `<报告名称>_YYYY-MM-DD.<扩展名>`，日期位于文件名末尾、扩展名前；使用报告实际日期，并与正文日期保持一致。
- 优先使用现有分类：商业分析、实机验证、仓库审查、界面设计；新主题按需增加分类。
- 新增或迁移报告时，同步更新 `docs/README.md` 索引和引用链接。迁移历史报告保留原报告日期。
- 截图、日志、安装包等证据保留在对应的本地输出目录，报告通过相对路径引用；报告正文统一在上述归档目录交付。
- 报告目录沿用本地报告的 Git 忽略策略。

## 模型分工与升级

- 默认保持单代理；明确、小范围的修改不派生子代理。
- 可独立拆分的只读代码路径探查、日志归纳或文档核对，使用 `lan_chat_scout`。主线程负责综合结论和后续实现。
- 涉及鉴权、事务、并发、WebSocket、消息一致性或跨端回归风险的改动，在实际改动形成后可使用 `lan_chat_reviewer` 做一次只读审查。
- 仅在已有可复现证据，并且两次基于不同证据的定位或修复仍未能解释问题；或故障同时跨越服务端、Vue 客户端与桌面/Android 客户端且无法定位责任边界时，使用 `lan_chat_astra_escalation`。它只负责诊断和验证策略，不直接修改文件。
- 同一时刻最多使用一个子代理。不要为了常规规划、简单修改或重复检查而使用 Astra 或并行子代理。
