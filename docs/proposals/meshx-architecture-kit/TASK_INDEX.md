# 实施任务索引

状态：以下全是待执行任务。本次编制不代表任何任务已在仓库完成。

| 任务 | 内容 | 前置 | 主要交付 |
|---|---|---|---|
| MX-A00 | 本地事实审查与回归基线 | 无 | audit、现有命令/结果、风险和交接 |
| MX-A01 | AI 工作区与轻量 workspace/验证入口 | A00；受影响基线可验证 | 短 AGENTS、索引、npm workspace、doctor/verify、CI 基础 |
| MX-A02 | v1 契约、地址/认证方案、共享测试向量 | A00、A01 | 真实契约、兼容政策、锁定生成链、认证 ADR |
| MX-A03 | TS Core 与 Web/Tauri 适配分层 | A02 | 逐模块抽取，原入口兼容，边界测试 |
| MX-A04 | 现有视觉 token 化与组件规范 | A01；与 A03 协調 shared 文件 | 同源 token、CSS 兼容别名、设计基线 |
| MX-A05 | Flutter 工程与真实文字聊天闭环 | A02、A03、A04 | Dart core、SDK、Theme、真实后端互通、重连 |
| MX-A06 | Flutter 系统能力与平台质量 | A05 | 文件/发现/通知/生命周期适配、真机记录 |
| MX-A07 | 功能对齐与发布就绪验收 | A06 | 明确功能矩阵、多端回归、升级/恢复验证 |
| MX-A08 | 可选：整理 apps/web、services/server 路径 | 前面关键回归稳定 | 纯路径迁移，不改变产品行为 |

## 使用规则

一轮只执行一张任务卡。需共享文件的任务先协调再并行。现有本地实现已经满足某项时，以证据标记复用，不重复创建。A08 可以不执行，未搬目录不妨碍轻量 Monorepo 达到目标。

实施状态记录在仓库 `docs/tasks/active/<任务号>.md`，这份索引描述计划而非实时状态。禁止把“完成计划文档”写成“完成任务”。
