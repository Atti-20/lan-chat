{
  "name": "meshx-contract-tooling",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node generate.mjs",
    "check": "node generate.mjs --check && node --test *.test.mjs && tsc --noEmit --strict --skipLibCheck --lib ES2022,DOM typecheck.ts"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "openapi-typescript": "7.13.0",
    "typescript": "5.9.3"
  }
}
